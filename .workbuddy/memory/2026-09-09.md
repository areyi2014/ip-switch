
## SKILL 国际化改造（2026-09-09 19:32）
- 按既定原则整理 skill i18n：SKILL.md 单语言（改为英文主文件）+ references/zh.md 中文版（按需加载）+ skill.json 保持双语元数据（description_zh/en）。
- install.sh / install.ps1 的 install_skill 均新增 references/ 子目录复制（WorkBuddy 副本 + Codex 镜像两处），bash -n 与 PS Parser 均通过。
- 已手动同步新 SKILL.md + references/zh.md 到 ~/.workbuddy/skills/ip-switch/ 与 ~/.codex/skills/ip-switch/（无需重跑 install）。
- 结论沉淀：skill 的读者是 agent，指令层不国际化；用户可见内容才按语言分文件；description 双语可提升触发命中率。

## INSTALL.md 双语拆分（2026-09-09 19:50）
- INSTALL.md 改为纯英文版，新建 INSTALL-zh.md 纯中文版；两文件顶部加 English | 简体中文 切换链接。
- 中文版「13 个 MCP 工具列表」链接指向 README-zh.md，英文版指向 README.md；路径占位符：中文版 <用户名>，英文版 <username>。

## SKILL.md 英文纯净化（2026-09-09 20:00）
- 用户指出 SKILL.md 示例短语残留中文。已全部改英文（"Open the ip-switch config page" / "Add an AWS account" / "Update the Azure credentials"、codex exec "Add an AWS account"）；顶部双语 description 精简为纯英文（中文描述由 skill.json description_zh 承担）。grep CJK 验证零残留。
- 中文话术保留在 references/zh.md。已同步到 ~/.workbuddy/skills/ip-switch/ 与 ~/.codex/skills/ip-switch/。

## 提交备注中文化→英文化（2026-09-09 22:05）
- 用 git filter-branch --msg-filter + sed 映射文件（.workbuddy/replacements.sed）重写最近10条提交备注（HEAD~10..HEAD 限定范围，更早 hash 不变）。
- 映射：SKILL.md改为全英文→Make SKILL.md fully English；安装过程实现中英文介绍→Add English and Chinese messages to installation；国际化 SKILL.md 并新增中文参考手册→Internationalize SKILL.md and add Chinese reference manual；readme文件制作为中英文版本→Create English and Chinese README versions；添加skill功能→Add skill support；命令行弹出窗口的隐藏→Hide command-line popup windows。
- force push 到 Gitee + GitHub 双远程成功（93125a79→d7bd89cf forced update）。
- 备份分支 backup-cn-messages 保留（指向旧历史 93125a79）；refs/original 已清理。第11条提交 0a58b277 仍是中文（用户未要求改）。
- 教训：Windows Git Bash 下 filter-branch 的 msg-filter 用正斜杠路径传 sed -f 可行；双 push URL 的 remote 一条 git push 同时推两个。

## 全史备注中译英（2026-09-09 22:40）
- 用 git filter-branch --msg-filter + node 纯文本替换脚本（.workbuddy/translate-msg.cjs + msg-map.tsv，82条规则）把全部151条中文提交备注译成英文，含最早 "first commit"。
- 踩坑：① filter-branch 要求工作区干净，每次改辅助脚本/映射后必须先 commit，否则报 unstaged changes（重定向输出时看不到）；② git stash -u 会把未跟踪辅助文件收走导致 msg filter "Cannot find module"；③ 提交信息里藏 \r 和 \n（多行 subject），映射键需转义支持；④ Write 工具生成的 TSV 首 行有 UTF-8 BOM，会吃掉第一条规则；⑤ "Ref is unchanged" 警告在 MSYS 上不可信，以实际 git log 为准。
- 残留：.git-rewrite/ 临时目录（~1140文件）被批量删除保护拦截未清掉，需确认后删除；dangling commit 为 stash drop 残留，无害。
- 已 force push 双远程：d7bd89cf→5416bba4。备份分支 backup-cn-messages 仍指向最初中文历史（93125a79）。
